// Find ASCII value of a character
// Use %d format specifier on char.

#include<stdio.h>
int main(void) {
    char ch;
    printf("Enter the character you wanna see ascii of :\n");
    scanf("%c",&ch);
    printf("The ascii character of enter character is %d",ch);
}